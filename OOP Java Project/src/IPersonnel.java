//This is our interface class. It has some methods for implements and using other personnel class.
public interface IPersonnel {
     boolean militaryStatus(boolean status);
     boolean graduationAverage(int avarage);
     boolean criminalRecordStatus(boolean criminalStatus);
}
